package com.brainlessmusic.app.ui.theme

import androidx.compose.ui.graphics.Color

// Loosely matches the web app's navy + orange re-theme (see
// .docs/STATUS.md's "Re-themed to flat navy blue + orange-red" entry) for
// visual family resemblance — not pixel-matched, since Material3's tonal
// system derives its own surface/container shades from these seeds.
val Navy900 = Color(0xFF0F172A)
val Navy800 = Color(0xFF1E293B)
val Navy700 = Color(0xFF334155)
val Orange500 = Color(0xFFF97316)
val Orange400 = Color(0xFFFB923C)
val Slate200 = Color(0xFFE2E8F0)
val Red400 = Color(0xFFF87171)
