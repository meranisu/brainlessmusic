# Compose reads this for JWT_SECRET and LIBRARY_DIR. Never commit it.
.env
.env.*
!.env.example

# Machine-specific absolute paths (JDK location etc.) — same reason
# android/local.properties and backend/.env stay out of git.
.vscode/settings.json
